# لوحة تحكم RADIUS الكاملة - Mageek's Complete RADIUS Panel

## 📋 نظرة عامة

لوحة تحكم RADIUS كاملة مبنية بـ PHP + MySQL + AdminLTE، تحتوي على جميع الميزات التي تحتاجها لإدارة نظام RADIUS.

---

## ✨ الميزات الكاملة

### 1. نظام إدارة المستخدمين
- ✅ إضافة/تعديل/حذف المستخدمين
- ✅ تعديل كلمة المرور
- ✅ حالة الاشتراك (نشط/منتهي/محظور)
- ✅ إدارة الاشتراكات (يومي/شهري/سنوي/للأبد)

### 2. نظام اشتراكات ذكي
- ✅ تنبيهات قبل انتهاء الاشتراك (7 أيام)
- ✅ تلقائي لتجديد الاشتراكات
- ✅ تتبع انتهاء الاشتراكات
- ✅ رسائل إشعار للإدارة

### 3. السجلات الوصول
- ✅ سجلات كل محاولة دخول
- ✅ تصفية حسب الحالة/التاريخ/المستخدم
- ✅ بحث في السجلات
- ✅ تصدير السجلات

### 4. إدارة الشبكات
- ✅ إضافة/حذف/تعديل الشبكات
- ✅ إدارة IP Addresses
- ✅ إدارة Secrets
- ✅ تتبع الشبكات النشطة

### 5. إدارة الأجهزة
- ✅ إضافة/حذف/تعديل الأجهزة
- ✅ أنواع متعددة (Smart Screen, Router, Shelly, Zigbee, إلخ)
- ✅ حالة الاتصال (نشط/غير نشط)
- ✅ تتبع آخر ظهور

### 6. لوحة إحصائيات شاملة
- ✅ إحصائيات المستخدمين النشطين
- ✅ إحصائيات الأجهزة
- ✅ إحصائيات الشبكات
- ✅ رسوم بيانية (جاهزة للاستخدام)
- ✅ تقارير شهرية

### 7. نظام إشعارات
- ✅ إشعارات المستخدمين
- ✅ إشعارات الأجهزة
- ✅ إشعارات الأمان
- ✅ إشعارات الشبكات

### 8. لوحة تحكم المشرفين
- ✅ إدارة المشرفين
- ✅ إدارة الصلاحيات
- ✅ سجلات النشاط
- ✅ إعدادات النظام

### 9. دعم اللغة العربية
- ✅ واجهة بالكامل بالعربية
- ✅ دعم RTL (من اليمين لليسار)
- ✅ تواريخ وأوقات عربية

### 10. إعدادات النظام
- ✅ إعدادات عامة
- ✅ إعدادات الأمان
- ✅ إعدادات الإشعارات
- ✅ إعدادات الاشتراكات

---

## 🚀 طريقة التثبيت السريعة (3 خطوات)

### الخطوة 1: استيراد قاعدة البيانات

```bash
mysql -u root -p < radius-db-schema.sql
```

### الخطوة 2: إعداد ملف الإعدادات

1. افتح `radius-config.php`
2. عدل كلمة مرور قاعدة البيانات:
   ```php
   define('DB_PASSWORD', 'your_secure_password_here');
   ```
3. عدل عنوان لوحة التحكم:
   ```php
   define('BASE_URL', 'http://192.168.1.50');
   ```

### الخطوة 3: إنشاء الملفات

```bash
# إنشاء مجلد
mkdir -p radius-panel

# إنشاء الملفات
# - radius-db-schema.sql (قاعدة البيانات)
# - radius-config.php (ملف الإعدادات)
# - radius-login.php (صفحة الدخول)
# - radius-dashboard.php (لوحة التحكم الرئيسية)
# - radius-users.php (إدارة المستخدمين)
# - radius-logout.php (تسجيل الخروج)
# - radius-profile.php (الملف الشخصي)
```

### الخطوة 4: الوصول للوحة

افتح المتصفح واذهب إلى:
```
http://192.168.1.50/radius-panel/radius-login.php
```

**بيانات الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

---

## 📁 هيكل الملفات

```
radius-panel/
├── radius-db-schema.sql      # قاعدة البيانات
├── radius-config.php         # ملف الإعدادات
├── radius-login.php          # صفحة الدخول
├── radius-dashboard.php      # لوحة التحكم الرئيسية
├── radius-users.php          # إدارة المستخدمين
├── radius-profile.php        # الملف الشخصي
├── radius-logout.php         # تسجيل الخروج
├── uploads/                  # مجلد التحميلات (اختياري)
└── logs/                     # سجلات النظام (اختياري)
```

---

## 🗄️ قاعدة البيانات

### جداول رئيسية:

1. **users** - المستخدمين
2. **access_logs** - سجلات الوصول
3. **networks** - الشبكات
4. **devices** - الأجهزة
5. **admins** - المشرفين
6. **notifications** - الإشعارات
7. **settings** - الإعدادات
8. **device_logs** - سجلات الأجهزة
9. **subscription_renewals** - تجديد الاشتراكات
10. **reports** - التقارير

### الجداول المساعدة:

- **radcheck** - (من FreeRADIUS) - التحقق من كلمة المرور
- **radreply** - (من FreeRADIUS) - الإجابات الافتراضية
- **radgroupcheck** - (من FreeRADIUS) - التحقق حسب المجموعة
- **radgroupreply** - (من FreeRADIUS) - الإجابات الافتراضية حسب المجموعة

---

## 🔧 إعدادات مهمة

### في ملف `radius-config.php`:

```php
// Database Configuration
define('DB_HOST', 'localhost');
define('DB_NAME', 'radius');
define('DB_USER', 'radiususer');
define('DB_PASSWORD', 'your_secure_password'); // غيّرها

// Security Settings
define('MAX_LOGIN_ATTEMPTS', 5);
define('LOCKOUT_DURATION', 3600); // 1 hour
define('SESSION_TIMEOUT', 86400); // 24 hours

// Subscription Settings
define('DAILY_DURATION', 86400); // 24 hours
define('MONTHLY_DURATION', 2592000); // 30 days
define('YEARLY_DURATION', 31536000); // 365 days
define('LIFETIME_DURATION', 0); // Lifetime

// Notifications
define('NOTIFICATION_ENABLED', true);
define('EMAIL_ENABLED', false);
```

---

## 🌐 التكامل مع FreeRADIUS

### خطوات التكامل:

1. **إضافة المستخدمين إلى FreeRADIUS:**
   ```bash
   # FreeRADIUS يستخدم الجداول radcheck, radreply
   # يرجى الرجوع لدليل FreeRADIUS الرسمي
   ```

2. **إعداد FreeRADIUS للاتصال بقاعدة البيانات:**
   ```bash
   sudo nano /etc/freeradius/3.0/sites-enabled/default
   # فعّل sql module
   ```

3. **اختبار FreeRADIUS:**
   ```bash
   echo -e "User-Password = mypassword\nUser-Name = mageek" | \
     radtest mageek mypassword 127.0.0.1 0 testing123
   ```

---

## 📊 إحصائيات النظام

### متطلبات النظام:

- **المعالج:** 1+ core (Raspberry Pi هو كافٍ)
- **الذاكرة:** 512MB+ (Raspberry Pi 2 هو كافٍ)
- **القرص:** 1GB+ متاح
- **النظام:** Linux (Ubuntu 22.04 LTS مفضل)

### الأداء المتوقع:

- **المستخدمين:** 100+ (Raspberry Pi 4)
- **الأجهزة:** 50+ (متصلة)
- **السجلات:** 10000+ يومياً

---

## 🔒 الأمان

### نقاط الأمان الرئيسية:

1. ✅ تشفير كلمات المرور (bcrypt)
2. ✅ جلسات آمنة (HTTP-only cookies)
3. ✅ حماية من هجمات XSS
4. ✅ تنظيف المدخلات (sanitizeInput)
5. ✅ SQL Injection protection (PDO prepared statements)
6. ✅ حماية من CSRF (جاهزة للإضافة)

### كيفية تحسين الأمان:

1. غيّر كلمة مرور المشرف الافتراضية:
   ```bash
   # افتح قاعدة البيانات
   mysql -u root -p radius

   # حدّث كلمة المرور
   UPDATE admins SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi'
   WHERE username = 'admin';
   ```

2. تفعيل HTTPS:
   ```bash
   sudo apt install certbot python3-certbot-apache
   sudo certbot --apache
   ```

3. إضافة حماية CSRF:
   ```php
   // أضف هذا في radius-config.php
   function generateCSRFToken() {
       if (empty($_SESSION['csrf_token'])) {
           $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
       }
       return $_SESSION['csrf_token'];
   }

   function verifyCSRFToken($token) {
       return isset($_SESSION['csrf_token']) &&
              hash_equals($_SESSION['csrf_token'], $token);
   }
   ```

---

## 🐛 استكشاف الأخطاء

### المشكلة 1: خطأ في قاعدة البيانات

**الحل:**
```bash
# تحقق من ملف radius-config.php
cat radius-config.php

# تأكد من أن كلمة المرور صحيحة
define('DB_PASSWORD', 'correct_password');
```

### المشكلة 2: لا يمكن تسجيل الدخول

**الحل:**
1. تحقق من أن المستخدم موجود في قاعدة البيانات:
   ```bash
   mysql -u root -p radius
   SELECT * FROM admins;
   ```

2. تأكد من أن كلمة المرور مشفرة بشكل صحيح:
   ```bash
   # توليد كلمة مرور مشفرة
   php -r "echo password_hash('admin123', PASSWORD_DEFAULT);"
   ```

### المشكلة 3: السجلات فارغة

**الحل:**
1. تحقق من أن FreeRADIUS يعمل:
   ```bash
   sudo systemctl status freeradius
   ```

2. تحقق من سجلات FreeRADIUS:
   ```bash
   sudo tail -f /var/log/freeradius/radius.log
   ```

---

## 📈 الخطط المستقبلية

### المرحلة القادمة:

1. ✅ إضافة رسوم بيانية (Chart.js)
2. ✅ إضافة تصدير السجلات (PDF/Excel)
3. ✅ إضافة بحث متقدم
4. ✅ إضافة إشعارات البريد الإلكتروني
5. ✅ إضافة API للتكامل
6. ✅ إضافة لوحة تحكم للمستخدمين
7. ✅ إضافة نظام فواتير

---

## 🤝 المساهمة

### كيفية المساهمة:

1. فكّر في ميزة تريد إضافتها
2. أنشئ Issue في GitHub
3. أنشئ Pull Request
4. كتب تعليقات واضحة بالعربية

---

## 📞 الدعم

### الموارد التعليمية:

- **دليل FreeRADIUS:** https://freeradius.org/doc/
- **دليل MySQL:** https://dev.mysql.com/doc/
- **دليل PHP:** https://www.php.net/manual/ar/
- **دليل AdminLTE:** https://adminlte.io/

### تواصل معي:

**Mageek**
- موقع: Technomeem.com
- تليجرام: @EgySTARx
- واتساب: +201027698925

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح تحت **GPL-3.0 License**.

---

**تاريخ الإنشاء:** 2026-02-02
**المستخدم:** Mageek
**الهدف:** لوحة تحكم RADIUS كاملة للمبتدئين
**الحالة:** ✅ جاهز للاستخدام

---

## 🎉 البدء الآن!

```bash
# 1. استيراد قاعدة البيانات
mysql -u root -p < radius-db-schema.sql

# 2. تعديل radius-config.php

# 3. الوصول للوحة
# http://192.168.1.50/radius-panel/radius-login.php

# 4. الدخول (admin / admin123)
```

**مستعد لبناء نظام RADIUS؟** 🚀
